{
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "restart": "powershell -ExecutionPolicy Bypass -File ./restart.ps1",
    "restart-simple": "npm run stop && npm run dev",
    "stop": "taskkill /f /im node.exe 2>nul || echo No Node processes found",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}